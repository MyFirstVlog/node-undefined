# Notas:
Primer programa en node
```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     This the base which the code will operate with
                                                             [number] [required]
  -l, --listar   List the operations                  [boolean] [default: false]
  -e, --edge     Param of the max lim of rows                           [number]
```